version: "3.8"
services:
  frontend:
    stdin_open: true
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:3000"
    volumes:
      - "/app/node_modules"
      - "./:/app"
